const TEMPLATE = {
  GET: {
    LIST: "/template/all",
    DETAIL: "/template/info",
  },
};

const MESSAGE = {
  POST: {
    SEND_ZNS: "/message/template",
  },
};

const API = {
  TEMPLATE,
  MESSAGE,
};

export default API;
